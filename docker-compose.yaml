services:
  web:
    build: .
    container_name: hanif_be
    restart: unless-stopped
    ports:
      - "127.0.0.1:${APP_PORT}:${APP_PORT}"
    env_file: 
      - .env
    networks:
      - postgres_network

networks:
  postgres_network:
    external: true